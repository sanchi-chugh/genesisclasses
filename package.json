{
  "name": "test-series",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "react-infinite-scroller": "^1.2.4"
  }
}
