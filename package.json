{
  "name": "test-series",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "@react-pdf/renderer": "^1.1.0",
    "webpack": "^4.28.2"
  }
}
